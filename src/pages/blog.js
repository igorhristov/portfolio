import React from "react";
import PropTypes from "prop-types";
import Layout from "../components/layout/Layout";

const Blog = (props) => {
  return (
    <Layout>
      <h1>Blog page</h1>
    </Layout>
  );
};

Blog.propTypes = {};

export default Blog;
